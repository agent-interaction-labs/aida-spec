{
  "name": "aida-spec",
  "version": "0.1.0",
  "description": "Agent Identity & Delegation Attestation — specification and JSON Schema",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "validate": "ajv validate -s schema/draft/schema.json -d schema/draft/examples/*.json",
    "check": "tsc --noEmit schema/draft/schema.ts"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
